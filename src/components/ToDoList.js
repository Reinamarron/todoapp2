import React from 'react'

function ToDoList(props){
  return(<li key={todoItem.id}><input checked={todoItem.completed} type="checkbox"/>{todoItem.title}</li>)

}

const styles{
  listItemStyles
  title:"Buy Groceries", 
  description:"party trays",
}

export default ToDoApp;